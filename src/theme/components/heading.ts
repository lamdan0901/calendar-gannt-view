import { defineStyleConfig } from '@chakra-ui/react';

export const headingTheme = defineStyleConfig({
  sizes: {
    xl: {
      fontSize: 'xl',
      lineHeight: '27px',
    },
    '2xl': {
      fontSize: '2xl',
      lineHeight: '27px',
    },
  },
});
